import { View } from "react-native"

export const VSpace = ({size = 5}: {size: number}) => <View style={{height: size}}></View>
export const HSpace = ({size = 5}: {size: number}) => <View style={{height: size}}></View>